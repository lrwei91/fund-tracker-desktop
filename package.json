{
  "name": "fund-tracker-desktop",
  "version": "0.1.23",
  "description": "恭喜发财本地 Electron 桌面应用",
  "main": "main.js",
  "private": true,
  "engines": {
    "node": ">=24 <27"
  },
  "scripts": {
    "start": "electron .",
    "check": "node --check main.js && node --check preload.js && node --check app/config-schema.js && for f in desktop/*.js renderer/*.js app/app.js app/api/*.js app/modules/*.js app/modules/watchlist/*.js; do node --check \"$f\"; done",
    "smoke": "node scripts/smoke-watchlist.js",
    "smoke:data": "node scripts/smoke-data.js",
    "version:bump": "node scripts/bump-version.js",
    "build:mac": "npm run build:mac:raw",
    "build:win": "npm run build:win:raw",
    "build:all": "npm run build:mac:raw && npm run build:win:raw",
    "build:mac:raw": "electron-builder --mac zip --publish never",
    "build:win:raw": "electron-builder --win portable --x64 --publish never",
    "lint": "eslint . --max-warnings 0",
    "format": "prettier --write \"app/**/*.js\" \"renderer/**/*.js\" main.js preload.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "overrides": {
    "form-data": "^4.0.6"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.4",
    "electron": "^43.1.0",
    "electron-builder": "^26.15.3",
    "eslint": "^9.39.4",
    "eslint-config-prettier": "^9.1.2",
    "globals": "^15.15.0",
    "jsdom": "^25.0.1",
    "prettier": "^3.9.4",
    "vitest": "^4.1.10"
  },
  "build": {
    "appId": "com.local.fundtracker",
    "productName": "恭喜发财",
    "directories": {
      "output": "dist"
    },
    "mac": {
      "target": "zip",
      "artifactName": "${productName}-${version}-mac-${arch}.${ext}",
      "category": "public.app-category.finance",
      "icon": "build/icon.icns"
    },
    "win": {
      "target": "portable",
      "icon": "build/icon.ico"
    },
    "portable": {
      "artifactName": "${productName}-${version}-win-${arch}-portable.${ext}"
    },
    "files": [
      "main.js",
      "preload.js",
      "desktop/**/*",
      "renderer/**/*",
      "app/**/*"
    ]
  }
}
