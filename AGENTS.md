# 恭喜发财桌面版协作约定

## 目标

在不破坏本地用户数据、安全边界或数据可靠性规则的前提下，以最小改动交付可验证的 Electron 桌面版功能与修复。

## 成功标准

- 改动解决用户提出的问题，不引入无关重构或新框架。
- 主窗口、浮窗和本地配置的数据契约保持兼容；已有用户配置可继续读取。
- 涉及数据源时，页面能如实表达来源、降级和不可用状态，不把未知、空值或成交额伪装成确定结论或净流入。
- 修改后运行与风险相称的检查，并报告已验证项、未验证项和剩余风险。

## 项目边界

- 保持原生 HTML/CSS/JavaScript + Electron 架构；不要迁移 React、TypeScript、Vite 或引入大型状态管理方案，除非用户明确要求。
- `main.js`、`preload.js` 和 `desktop/` 负责主进程、受限 IPC、配置存储和协议路由；renderer 不得直接获得 Node 或任意文件系统权限。
- `app/api/_gateway.js` 是上游请求的统一入口。新增或调整外部请求时复用其并发、限速、熔断与降级能力。
- 持久用户数据通过异步 IPC 写入 `config.json`；`localStorage` 仅用于临时缓存。变更配置结构时保持迁移/旧字段兼容。
- `dist/` 为构建产物，不入库；打包命令不得隐式修改版本号。

## 工作方式

- 先检查相关入口、调用链、测试和现有实现；能从仓库或命令输出确认的事实不靠猜测。
- 优先复用现有模式与 helper，只修改完成目标所必需的文件。
- 对多步骤任务，在首次操作前说明目标和验证方式；阶段切换时再更新，避免过程性刷屏。
- 工作区存在无关改动时保留它们，不回滚或顺手整理。

## 验证规则

- 纯逻辑或 API 改动：至少运行相关测试；无法定位时运行 `npm test`。
- 修改 JavaScript：运行 `npm run check`；涉及格式或规则时再运行 `npm run lint`。
- 修改自选、持仓、浮窗或初始化链路：运行 `npm run smoke`。
- 涉及真实第三方数据时，可运行 `RUN_LIVE_DATA=1 npm run smoke:data`；网络、休市或上游风控导致未运行/失败时明确说明，不将其视为代码正确性结论。
- 修改打包或 Electron 主进程时，运行相应 `build:*:raw` 或说明未打包的原因。

## 外部操作与停止条件

- 本地只读检查和本地代码修改可直接执行。
- push、创建 GitHub Release、上传安装包、修改远程配置、删除文件或覆盖用户数据前，先取得明确确认；已确认的同一发布流程内可继续完成必要步骤。
- 需求、数据口径或目标环境缺少会显著改变结果的关键信息时，停止并说明需要的决策；其余可从现场确认的细节自行处理。
- 完成后用中文给出结论：改了什么、验证了什么、未验证什么、剩余风险或下一步。
