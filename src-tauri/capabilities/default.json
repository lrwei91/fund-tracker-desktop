{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Only event listening and frameless holding-window dragging.",
  "windows": ["main", "holding", "alert"],
  "permissions": ["core:event:default", "core:window:allow-start-dragging"]
}
