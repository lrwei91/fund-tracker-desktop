{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Event listening, window dragging, and native theme synchronization.",
  "windows": ["main", "holding", "alert"],
  "permissions": [
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-theme"
  ]
}
