{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "holding-window",
  "description": "Holding widget event, drag-region and native theme permissions.",
  "windows": ["holding"],
  "permissions": [
    "core:event:default",
    "core:window:allow-start-dragging",
    "core:window:allow-set-theme"
  ]
}
